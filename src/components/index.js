import {
  TouchableOpacity,
  TouchableWithoutFeedback,
  TouchableHighlight,
  TouchableNativeFeedback
} from 'react-native'

import Touchable from './Touchable'

export {

  // Proxies
  TouchableOpacity,
  TouchableWithoutFeedback,
  TouchableHighlight,
  TouchableNativeFeedback,

  // Customs
  Touchable

}
